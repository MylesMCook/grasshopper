{
  "mcpServers": {
    "grasshopper": {
      "type": "stdio",
      "command": "ABSOLUTE_GRASSHOPPER_BINARY",
      "args": [
        "bridge",
        "--config",
        "ABSOLUTE_CLIENT_CONFIG"
      ]
    }
  }
}
