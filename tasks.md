# Grasshopper release state

Owner: Codex. Machine: Mac mini. Checkout: `/Users/mylescook/Code/MylesMCook/grasshopper`, branch `main`. Beelink has a separate host-maintenance task. Preserve unrelated ignored build and browser files.

## Current release

- [2.2.0](https://github.com/MylesMCook/grasshopper/releases/tag/v2.2.0) is public from `f07ba78`. The client `setup` command checks the private server before connecting selected agents; `--update` replaces Grasshopper-only wiring with rollback on failure. The server archive has a checked quickstart backup command. The [live setup page](https://usegrasshopper.com/setup/) links to all six downloads.
- [CI](https://github.com/MylesMCook/grasshopper/actions/runs/36099946990) passed on Mac, Linux, and Windows. All six archives match their native CI binaries and 288 internal checksums. Extracted Mac and Beelink Linux packages passed quickstart, authentication, client setup, backup integrity, and outage checks. Isolated Codex and Claude native plugin install/update/removal passed on Mac. [Evidence](docs/memory-acceptance.md).
- The running Mac mini service and database were not updated. The published Windows 2.2.0 archive has CI coverage but no physical-host smoke test; a same-version marketplace candidate was tested on Work HP. Desktop/IDE behavior still needs separate checks. Codex hook trust and Cursor MCP approval remain native steps.

## Shipped and checked

- [2.1.0](https://github.com/MylesMCook/grasshopper/releases/tag/v2.1.0) is public from `47fb666`. Seven uploaded asset digests matched the six checked archives and external `SHA256SUMS`. [Final CI](https://github.com/MylesMCook/grasshopper/actions/runs/36094500976) passed Mac, Linux, and Windows. Extracted server archives ran with isolated data on Mac mini, Beelink, and Work HP. Physical Windows client `configure`, `check`, Cursor wiring, and bounded outage passed. All test servers are stopped.
- [usegrasshopper.com/setup/](https://usegrasshopper.com/setup/) is live at Worker version `757304d5-06c1-470d-b71a-11e31ee73ac5`. Its client-guide link opens the current GitHub README. All six server/client downloads returned 200. Live browser navigation, theme, three client links, 390px width, and console checks passed; public MCP and private-view routes returned 404.
- All six tracked READMEs now have separate jobs: project entry, client setup, manual wiring, Codex Git marketplace, website operations, and font licenses. Relative links, bundled-guide links, GitHub rendering, tests, and the site dry run passed. Commit `6d3e96e` is pushed; the older 2.1.0 archive still contains its original INSTALL.md.
- The published Mac client is installed in the Grasshopper application folder. Its config points to the existing private loopback server and token file without copying the token. Fresh Codex, Cursor, and Claude Code CLI turns received confirmed global record 8 revision 1 before tools. Codex created synthetic project record 9; Cursor corrected it at revision 2; Codex read both revisions and archived it at revision 3. Active project context then omitted it. No confirmed preference was changed.
- Isolated Codex and Claude Code profiles installed and removed both an earlier task-local and the final package. Cursor updated from the earlier binary to the final path, then removed Grasshopper while retaining unrelated MCP and hooks. The real Mac installs remain active.
- The private Mac mini service still runs its earlier checked binary with encrypted off-host backups and a signed 30-day viewer session. Release 2.2.0 has **not** replaced that live binary. [Behavior and recovery evidence](docs/memory-acceptance.md).

## Next checks

- **Marketplace candidate:** The public `marketplace-candidate` branch carries OS-specific Codex/Cursor plugins for Mac arm64, Windows x64, and Linux x64. `setup --agents none` authenticates before saving one shared client config. No token or memory database is packaged. Mac Codex and Cursor passed fresh first-turn context, correction/readback, bounded outage, and native Git install/removal. Work HP Codex passed the same first-turn and outage checks. Beelink Cursor passed native Git install and one fallback MCP `context` call in a fresh turn; Beelink Codex passed native Git install/update/removal. [Exact evidence and limits](docs/memory-acceptance.md#marketplace-pilot-25-september).
- **Windows Cursor fix:** Cursor CLI 2026.09.23 has a project-level MCP approval bug: management commands list five ready tools while Agent sessions show none. In a task-local **user-level** Cursor config, fresh Agent turns called `context`, corrected synthetic record 1 through MCP `store` from revision 2 to 3, read current/prior content through `get`, and handled a stopped backend in 32.1s without a false save. The bundled `setup --cursor-cli` placed the MCP entry at that user level by default. A combined native-plugin plus user-level MCP fresh turn read revision 3 in 34.3s. Keep that default on Windows; do not require broad `--approve-mcps`. [Evidence](docs/memory-acceptance.md#marketplace-pilot-25-september).
- **Changed-version update:** Mac Codex upgraded its isolated Git plugin from 2.2.0 to 2.2.1. Cursor's `marketplace update` stayed pinned to the old commit; remove/re-add plus plugin reinstall loaded 2.2.1. `setup --update` repointed scratch MCP wiring to the new binary and authenticated. [Evidence](docs/memory-acceptance.md#marketplace-pilot-25-september). The public guide uses this verified Cursor workaround.
- **Other open checks:** Codex compaction/subagent refresh, Desktop/IDE behavior (user-led if an issue appears), reboot persistence, recovery-note sync on another device, and Mac-loss recovery. Legacy-data migration is separate and unapproved. Codex hook trust and Cursor MCP approval remain native security steps.
- To update the live Mac service to the 2.2.0 binary and remove its obsolete local `/about/` route, verify a current backup and rollback binary, then obtain approval for that specific restart.
