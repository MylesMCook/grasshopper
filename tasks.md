# Grasshopper release state

Owner: Codex. Mac checkout: `/Users/mylescook/Code/MylesMCook/grasshopper`, branch `main`. Public-site worktree: `/Users/mylescook/.codex/worktrees/grasshopper-cloudflare-site/grasshopper`, branch `codex/cloudflare-site`. Beelink is shared with another host-maintenance task. Preserve unrelated `.build/` and `.playwright-cli/` folders.

## Done

- The static public site is live at `https://usegrasshopper.com` ([source PR #4](https://github.com/MylesMCook/grasshopper/pull/4)). Its setup path is labeled private pilot; `/view/` opens a visitor's own server and stores only that server address in the browser. Local and live Chromium navigation, 390px layout, theme, address validation, CSP, and public 404 checks passed. Cloudflare analytics injection is suppressed with `no-transform`; live browser console is clear. The site does not route to the private memory service.
- The public site's Codex annotation fix is deployed as Worker version `aec0df5b-4f88-48ec-ad1f-313b44f0dc15`. Live `/`, `/view/`, and 404 responses include the two exact style hashes already proven on the private memory view. The first header-only deploy kept the old edge policy; the build now changes page asset hashes with `_headers`, and the second deploy returned the new policy on all three routes. The user selected headings on both public pages in Codex Annotate after refresh.
- [2.0.2](https://github.com/MylesMCook/grasshopper/releases/tag/v2.0.2) is published with the current memory view in six checked portable archives. [Final CI](https://github.com/MylesMCook/grasshopper/actions/runs/36079552814) passed on Mac, Windows, and Linux. Extracted Mac and Linux servers passed synthetic authentication and view smoke checks; uploaded asset digests match local files. Windows 2.0.2 ran in CI, not on a physical Windows host.
- [2.0.1](https://github.com/MylesMCook/grasshopper/releases/tag/v2.0.1) is published with six checked Mac, Windows, and Linux archives from `dd15c15`; final [CI](https://github.com/MylesMCook/grasshopper/actions/runs/36067286155) passed.
- The checked Mac server runs privately with encrypted off-host backups. After synthetic pilot cleanup, the latest off-host restore matched the source bytes, passed SQLite integrity, and retained the archived record and its prior revision. No legacy records were migrated.
- A fresh 24 September R2 backup restored byte-for-byte; SQLite integrity passed, and a separate server read current and prior revisions. The live service was not interrupted.
- The memory view now browses active global memories across devices, offers known project and device IDs with manual entry, and explains cross-device context. Commit `5e19b96` is pushed; Mac, Windows, and Linux CI passed. Synthetic HTTP and Chromium checks covered scope, project isolation, and 390px layout. The Mac mini now runs this clean-commit binary on its existing private route; authenticated health, view API, and historical MCP read passed after restart. No active records remain after pilot cleanup.
- Mac mini and Beelink clones with the same Git `origin`, different paths, and different checkout commits independently resolved the same project ID. Synthetic project-only records traveled in both directions through the live service; an unrelated project saw none. Both records were archived with history intact. See [acceptance evidence](docs/memory-acceptance.md).
- Fresh Codex and Cursor CLI turns on Mac mini and Beelink independently read the same live project-only record. Cursor headless reads needed `--auto-review`; the packaged exact read allowlist passed on Mac. The record is archived with history available. A cross-clone regression test covers credential stripping, HTTPS/SSH remote normalization, and project isolation.

## Open checks

- Public server downloads and setup instructions remain unavailable while this repository is private. The site's setup button explains the pilot status rather than linking to a private release. A fresh-install public launch needs a separate packaging/publication decision.
- Grasshopper's own repo marketplace and Codex plugin are pushed under `.agents/plugins/` and `plugins/grasshopper/`; a fresh Git-backed install remains untested. The 2.1.0 draft has seven uploaded assets with matching checksums, and final Mac/Linux/Windows CI passed. The source adds `/about/`, a shared light/dark control, and a 30-day signed browser session. Synthetic browser/API, extracted Mac server, Go test/vet/race, JavaScript syntax, and manifest validation passed. A current live binary/plist rollback copy and new binary are staged in the private site-rollout task folder; the requested Host Safety approval for restart and user-level marketplace install is pending. Linux/Windows 2.1.0 packages were not run on physical hosts; Beelink SSH halted at host-key verification.
- The compact visualizer and opt-in Codex annotation CSP fix are live on the Mac mini. Synthetic Chromium checks covered stable selection, deferred updates, details, and 390px reflow. The live tab first logged two blocked inline styles and a collapsed annotation layer; with only those two hashes allowed, the user selected the heading in both task-local and live Codex annotation mode. After the approved live restart, local/private health and auth passed, and memory counts remained unchanged. The prior binary/plist are in the private visualizer task folder. This task has no callable Grasshopper MCP plugin, so the first real memory awaits a Codex restart/new task. No substitute write was made.
- Prioritize Codex across machines and Codex ↔ Cursor on the same and different machines. Work HP model-turn timeouts are not a release gate: the hooks reached the service, and no Grasshopper defect was isolated. Leave Work HP alone until the user asks for another check; desktop checks are user-led if an issue appears.
- Codex compaction and independent subagent hook delivery remain unisolated; fresh and resume behavior already passed. Cursor's native Git marketplace install remains untested. The later Beelink CLI pilot used a verified SSH hostname and host key.
- Reboot persistence, Bitwarden recovery-note sync on another device, and Mac-loss recovery remain untested. Reboot or persistent service changes require separate Host Safety approval. Legacy-data migration remains separate and unapproved.
